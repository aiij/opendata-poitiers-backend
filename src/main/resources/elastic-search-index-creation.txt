curl -XPOST 'http://localhost:9200/open-data-poitiers/' -d '{
    "settings" : {
        "number_of_shards" : 1
    },
    "mappings" : {
        "bike-shelters" : {
            "properties" : {
                "type" : { "type" : "string", "index" : "not_analyzed" },
                "location" : { "type" : "geo_point" }
            }
        }
    }
}'